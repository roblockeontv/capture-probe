# Capture probe

A diagnostic page, not a product. It settles whether a browser camera works inside an installed home-screen web app, and whether occluded window capture works on a desktop. Open `capture-probe.html`, add to home screen, open from the icon, press Test camera, copy the report. The page prints the sha256 it was served.
